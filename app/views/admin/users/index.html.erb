<h2>Users <%= link_to "Create New",  new_admin_user_url, {:class => "button add", :title => "Add an user"} %></h2>
<table class="tableB">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Email Address</th>
      <th scope="col">State</th>
      <th scope="col">Created At</th>
    </tr>
  </thead>
  <tfoot>
    <tr>
      <td colspan="4">
      <%= will_paginate @users %>
      </td>
    </tr>
  </tfoot>
  <tbody>

    <% @users.each do |user| %>

    <tr<%= cycle(' class="pair"','') -%>>
      <td><strong><%= link_to user.login, edit_admin_user_url(user), {:title => "Edit user"} -%></strong></td>
      <td><%= mail_to user.email, user.email, {:title => "Email this user"} -%></td>
      <td><%= user.state -%></td>
      <td><%= user.created_at -%></td>
    </tr>

    <% end %>
    
  </tbody>
</table>
<div class="info_results">
    <span class="info_items">60 usuarios de 240 en la lista</span>
    <span class="pages_navigation right">
        <a href="#">&lt;Previous</a>
        <a href="#">1</a>
        <a href="#">2</a>
        <strong>3</strong>
        <a href="#">4</a>
        <a href="#">Next&gt;</a>
    </span>
</div>

